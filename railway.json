{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "pip install -r requirements.txt",
      "outputDirectory": "."
    },
    "deploy": {
      "startCommand": "cd /app && python bot.py"
    }
  }